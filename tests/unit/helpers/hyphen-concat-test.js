import { hyphenConcat } from '../../../helpers/hyphen-concat';
import { module, test } from 'qunit';

module('Unit | Helper | hyphen concat');

// Replace this with your real tests.
test('it works', function(assert) {
  const result = hyphenConcat(42);
  assert.ok(result);
});
